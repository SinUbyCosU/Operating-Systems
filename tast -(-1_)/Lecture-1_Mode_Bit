import time

class CPU:
    def __init__(self):
        self.mode_bit=1

    def read_kernel_memory(self):
        if self.mode_bit=1:
            return "ACCESS DENIED"
        else:
            return"Kernel Data Accessed:{KERNEL PASSWORD}"

        def system_call(self):
            print("System call Triggered")
            print("CPU Triggered switching Mode Bit 1 to 0")
            self.mode_bit=0

            print(self.read_kernel_memory())

            print("CPU Task done switching Mode Bit 0 to 1")
            self.mode_bit=1
            
        CPU()
        while True:
            print(f"Current Mode bit:{cpu.mode_bit}")
            action=input("Type 'Hack' to trigger system call:")

        if action.lower()=="hack":
            print("Attempting to read Kernel Memory...")
            print(cpu.read_kernel_memory())

        elif action.lower == "ask":
            cpu.system_call()
        elif action=='exit':
            break